# Production API Configuration
VITE_API_BASE_URL=https://api.production-server.com/v1

# Authentication
# Ensure the token is stored in localStorage as 'prod_auth_token'
# or implement a formal OAuth/OpenID flow.

# Environment
NODE_ENV=production
